specVersion: "v1beta"
serviceId: "ios"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ios-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
